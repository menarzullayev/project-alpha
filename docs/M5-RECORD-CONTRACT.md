# Project Alpha — Canonical Record Contract

All material evidence, decisions, approvals, and stage handoffs have two representations:

1. Canonical Markdown record used by humans and deterministic audits.
2. Immutable event JSON used for workflow history.

Agents must not treat event history as a substitute for a canonical record when a record schema is required by the integrity gate.
