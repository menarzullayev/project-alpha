# Quality Gate — 05 Domain Model

PASS only when core entities, value objects, aggregates, relationships, invariants, business rules, and lifecycle rules are explicit and traceable to PRD requirements.

BLOCK if domain boundaries or critical business rules are contradictory or unresolved.
